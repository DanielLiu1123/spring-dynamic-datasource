spring:
  datasource:
    name: postgres1
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/testdb
    username: root
    password: root
    datasources:
      - name: postgres2
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/testdb
        username: root
        password: root

logging:
  level:
    examples.mybatis: debug
    dynamicds: debug
